/** Board dimensions */
export const BOARD_SIZE = 8;

/** Pixel size of each tile */
export const TILE_SIZE = 64;

/** Board offset from top-left to center it in 800x600 */
export const BOARD_OFFSET_X = (800 - BOARD_SIZE * TILE_SIZE) / 2;
export const BOARD_OFFSET_Y = (600 - BOARD_SIZE * TILE_SIZE) / 2 + 20;
